var React = require('react');

/**
 * ElseIf component to be used with If
 */
var ElseIf = React.createClass({
  // don't render anything with ElseIf
  render: function () {
    return null;
  }
});

module.exports = ElseIf;
