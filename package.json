{
  "name": "react-if-else",
  "version": "1.1.1",
  "description": "If/Else rendering components for React",
  "repository": {
    "type": "git",
    "url": "https://github.com/kimagure/react-if-else.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "scriptPreprocessor": "<rootDir>/jest/preprocessor.js",
    "unmockedModulePathPatterns": [
      "<rootDir>/node_modules/react"
    ]
  },
  "author": "Justin Woo",
  "license": "MIT",
  "devDependencies": {
    "jest-cli": "^0.4.0",
    "react": "^0.12.0",
    "react-tools": "^0.12.0"
  },
  "peerDependencies": {
    "react": ">=0.11.0"
  }
}
